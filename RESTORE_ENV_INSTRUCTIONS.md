# Инструкция по восстановлению файла .env на сервере

## 🚨 Проблема

Файл `.env` пропал на удаленном сервере.

## ✅ Решение

### Вариант 1: Автоматическое восстановление (рекомендуется)

1. Подключитесь к серверу по SSH
2. Загрузите скрипт `restore-env.sh` на сервер или выполните команды вручную:

```bash
# Перейти в директорию проекта
cd /var/www/www-root/data/www/m.dekan.pro

# Скопировать .env.example в .env
cp .env.example .env

# Определить пользователя PHP-FPM
WORKER_USER=$(ps aux | grep "php-fpm: pool" | grep -v grep | head -1 | awk '{print $1}')
if [ -z "$WORKER_USER" ]; then
    if id "www-root" &>/dev/null; then
        WORKER_USER="www-root"
    else
        WORKER_USER="www-data"
    fi
fi

# Установить права доступа
chown $WORKER_USER:$WORKER_USER .env
chmod 640 .env

# Сгенерировать APP_KEY
php artisan key:generate --force

# Очистить кеш
php artisan config:clear
php artisan cache:clear
```

### Вариант 2: Ручное восстановление

1. **Подключитесь к серверу по SSH**

2. **Перейдите в директорию проекта:**

    ```bash
    cd /var/www/www-root/data/www/m.dekan.pro
    ```

3. **Создайте .env файл из шаблона:**

    ```bash
    cp .env.example .env
    ```

4. **Отредактируйте .env файл:**

    ```bash
    nano .env
    # или
    vi .env
    ```

5. **Обязательно настройте следующие переменные:**

    ```env
    APP_NAME="Education Management System"
    APP_ENV=production
    APP_KEY=
    APP_DEBUG=false
    APP_URL=https://m.dekan.pro

    DB_CONNECTION=mysql
    DB_HOST=127.0.0.1
    DB_PORT=3306
    DB_DATABASE=your_database_name
    DB_USERNAME=your_database_user
    DB_PASSWORD=your_database_password

    MAIL_MAILER=smtp
    MAIL_HOST=smtp.example.com
    MAIL_PORT=587
    MAIL_USERNAME=your_email@example.com
    MAIL_PASSWORD=your_email_password
    MAIL_ENCRYPTION=tls
    MAIL_FROM_ADDRESS=your_email@example.com
    MAIL_FROM_NAME="${APP_NAME}"
    ```

6. **Сгенерируйте APP_KEY:**

    ```bash
    php artisan key:generate --force
    ```

7. **Установите правильные права доступа:**

    ```bash
    # Определить пользователя PHP-FPM
    WORKER_USER=$(ps aux | grep "php-fpm: pool" | grep -v grep | head -1 | awk '{print $1}')
    if [ -z "$WORKER_USER" ]; then
        WORKER_USER="www-root"  # или "www-data" в зависимости от вашего сервера
    fi

    chown $WORKER_USER:$WORKER_USER .env
    chmod 640 .env
    ```

8. **Очистите кеш Laravel:**

    ```bash
    php artisan config:clear
    php artisan cache:clear
    php artisan view:clear
    php artisan route:clear
    ```

9. **Перезапустите PHP-FPM (если нужно):**

    ```bash
    # Для PHP 8.4
    systemctl restart php84-php-fpm

    # Или для PHP 8.3
    systemctl restart php8.3-fpm
    ```

## 🔍 Проверка

После восстановления проверьте:

1. **Проверьте, что файл существует:**

    ```bash
    ls -la .env
    ```

2. **Проверьте права доступа:**

    ```bash
    ls -l .env
    # Должно быть: -rw-r----- (640)
    ```

3. **Проверьте, что APP_KEY сгенерирован:**

    ```bash
    grep APP_KEY .env
    # Должно быть: APP_KEY=base64:...
    ```

4. **Проверьте работу приложения:**
   Откройте сайт в браузере и убедитесь, что он работает корректно.

## ⚠️ Важные замечания

-   **Безопасность:** Файл `.env` содержит конфиденциальную информацию. Убедитесь, что права доступа установлены правильно (640 или 600).
-   **Резервная копия:** Рекомендуется сохранить резервную копию `.env` в безопасном месте.
-   **База данных:** Убедитесь, что настройки базы данных в `.env` соответствуют реальной конфигурации сервера.
-   **APP_KEY:** Если у вас была резервная копия старого `.env`, используйте старый APP_KEY, иначе пользователям придется заново входить в систему.

## 🆘 Если что-то пошло не так

1. Проверьте логи Laravel:

    ```bash
    tail -f storage/logs/laravel.log
    ```

2. Проверьте права доступа на директории:

    ```bash
    ls -la storage/
    ls -la bootstrap/cache/
    ```

3. Убедитесь, что PHP может читать `.env` файл:
    ```bash
    php artisan config:show
    ```
