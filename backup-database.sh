#!/bin/bash

# Скрипт для ручного создания резервной копии базы данных
# Использование: ./backup-database.sh

echo "🔄 Создание резервной копии базы данных..."

# Переход в директорию проекта
cd "$(dirname "$0")"

# Проверка наличия artisan
if [ ! -f "artisan" ]; then
    echo "❌ Ошибка: файл artisan не найден. Убедитесь, что вы находитесь в корне проекта Laravel."
    exit 1
fi

# Создание резервной копии
php artisan db:backup

# Проверка результата
if [ $? -eq 0 ]; then
    echo ""
    echo "✅ Резервная копия успешно создана!"
    echo ""
    echo "💡 Совет: Перед применением миграций или важных изменений"
    echo "   рекомендуется создать резервную копию вручную."
else
    echo ""
    echo "❌ Ошибка при создании резервной копии!"
    exit 1
fi

