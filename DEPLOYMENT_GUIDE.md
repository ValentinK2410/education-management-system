# 🚀 Руководство по деплою проекта

## Доступные скрипты деплоя

### 1. `deploy-simple.sh` - Основной скрипт деплоя
Автоматически обновляет проект на сервере с GitHub и выполняет все необходимые операции.

**Использование:**
```bash
./deploy-simple.sh
```

**Что делает:**
- 📥 Обновляет код с GitHub
- 📦 Устанавливает зависимости через Composer
- 🧹 Очищает кэш Laravel
- 🗄️ Запускает миграции базы данных
- ⚡ Оптимизирует приложение
- 🔐 Устанавливает правильные права доступа

### 2. `deploy.sh` - Альтернативный скрипт (требует sshpass)
```bash
./deploy.sh
```

### 3. `sync-to-server.sh` - Простая синхронизация
```bash
./sync-to-server.sh
```

## Конфигурация сервера

- **Хост:** 82.146.39.18
- **Пользователь:** root
- **Путь проекта:** /var/www/www-root/data/www/m.dekan.pro
- **GitHub репозиторий:** https://github.com/ValentinK2410/education-management-system.git

## Требования

- SSH доступ к серверу
- Установленный Git на сервере
- Composer на сервере
- PHP 8.2+ на сервере

## Процесс деплоя

1. Внесите изменения в код локально
2. Зафиксируйте изменения: `git add . && git commit -m "Описание изменений"`
3. Загрузите на GitHub: `git push origin main`
4. Запустите деплой: `./deploy-simple.sh`

## Проверка статуса

После деплоя проект будет доступен по адресу: http://82.146.39.18

## Устранение неполадок

Если деплой не работает:
1. Проверьте SSH соединение: `ssh root@82.146.39.18`
2. Убедитесь, что пароль правильный
3. Проверьте права доступа к файлам на сервере
4. Проверьте логи Laravel: `tail -f storage/logs/laravel.log`
